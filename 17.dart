void main(List<String> args) {
  var list = [];
  // list.addAll(["Al","Mosrof","Shinul"]);
  list.insertAll(0, ["Shinul", "Mia", "Al", '12']);
  print(list);

  // here insertAll used for list with index
}
